# Useful Git Commands

1. **git status** – Shows changed files and the current branch.
2. **git add** – Stages changes to be committed.
3. **git commit** – Saves staged changes to the repository history.
4. **git log** – Shows the commit history.
5. **git push** – Uploads local commits to a remote repository.
- git merge: integrates changes from another branch
